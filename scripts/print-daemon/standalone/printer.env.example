# Copy to  printer.env  (same folder) and fill in. printer.env is gitignored.

# Supabase → Project Settings → API
SUPABASE_URL=https://upxgcpkatrcejicailmp.supabase.co
# SERVICE ROLE key (the secret one). ⚠ Use a freshly ROTATED key — never share it.
SUPABASE_SERVICE_ROLE_KEY=PASTE_ROTATED_SERVICE_ROLE_KEY

# Kitchen printer (Star SP742)
PRINTER_HOST=10.1.10.200
PRINTER_PORT=9100
PRINTER_MODE=starline

POLL_MS=2000
LOOKBACK_HOURS=6
