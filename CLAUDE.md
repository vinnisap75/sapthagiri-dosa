Dont start coding until i explicitly tell yes start coding

Flag a security/credential concern (e.g. a key that needs rotating) ONCE — or file an issue for it — then move on. Do not repeat the same warning in later messages. The reminder being logged is enough; re-raising it every turn is noise.
