node_modules/
.next/
out/
.env*.local
# Print-daemon secret (Supabase service-role key) — never commit.
scripts/print-daemon/printer.env
scripts/print-daemon/standalone/printer.env
scripts/print-daemon/standalone/node_modules/
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Private staff-only planning. Do NOT commit. Contains the Sentry audit,
# orchestrator HANDOFF.md with operational state, and runtime worktrees.
# The COMMITTED agent docs (prompts, protocol, plan, night logs) live under
# docs/agents/ + docs/PLAN.md + docs/NIGHT_LOG/ + docs/AGENT_QUESTIONS.md.
.agent-org/

# Vercel (linked project + pulled env — never commit)
.vercel
.env*.production.local
